# ==========================================
# ZIP-to-Paperless Konfiguration
# ==========================================

# --- IMAP Einstellungen (Posteingang) ---
IMAP_SERVER=imap.example.com
IMAP_PORT=993
IMAP_USER=deine-email@example.com
IMAP_PASSWORD=dein-passwort

# --- SMTP Einstellungen (Weiterleitung) ---
SMTP_SERVER=smtp.example.com
SMTP_PORT=587
SMTP_USER=deine-email@example.com
SMTP_PASSWORD=dein-passwort

# --- Ziel-Konfiguration ---
TARGET_EMAIL=paperless-inbox@example.com

# --- Workflow Einstellungen ---
# Welcher Ordner soll überwacht werden? (Standard: INBOX)
SOURCE_FOLDER=INBOX

# Sollen die Mails nach der Verarbeitung gelöscht werden? (true / false)
# Wenn 'false', werden sie nur als "gelesen" markiert.
DELETE_AFTER_PROCESSING=false

# --- Filter (Optional) ---
# Nur Mails von bestimmten Absendern verarbeiten (leer lassen für alle)
# Beispiel: FILTER_SENDER=rechnungen@amazon.de
FILTER_SENDER=

# Nur Mails mit bestimmtem Text im Betreff verarbeiten (leer lassen für alle)
# Beispiel: FILTER_SUBJECT=Rechnung
FILTER_SUBJECT=